# USA 决议

foreign_support = { #援助中国

	CHI_flying_tigers = { #派遣飞虎队

		icon = generic_air

		allowed = {

			original_tag = USA

		}

		available = {

			has_equipment = { small_plane_airframe > 99 }

		}

		visible = {

			has_country_flag = CHI_flying_tigers

		}

		cost = 25

		ai_will_do = {

			factor = 1

		}

		fire_only_once = yes

		days_remove = -1

		complete_effect = {

			random_country = {

				limit = {

					has_completed_focus = CHI_invite_the_flying_tigers

				}

				custom_effect_tooltip = flying_tigers_tt

				hidden_effect = {

					if = {

						limit = {

							NOT = { has_dlc = "By Blood Alone" }

						}

						add_equipment_to_stockpile = {

							producer = USA

							type = fighter_equipment_1

							amount = 100

						}

					}

					else = {

						USA = { #美国

							create_equipment_variant = {

								name = "P-40" #

								type = small_plane_airframe_2

								modules = {

									fixed_main_weapon_slot = heavy_mg_4x

									fixed_auxiliary_weapon_slot_1 = heavy_mg_2x

									fixed_auxiliary_weapon_slot_2 = bomb_locks

									engine_type_slot = engine_3_1x

									special_type_slot_1 = drop_tanks

								}

							}

						}

						add_equipment_to_stockpile = {

							producer = USA

							type = small_plane_airframe_2

							amount = 100

							variant_name = "P-40"

						}

					}

				}

			}

			add_war_support = 0.05

		}

		modifier = {

			air_volunteer_cap = 1

		}

	}

	CHI_soviet_volunteer_group = { #派遣苏联志愿航空队

		icon = generic_air

		allowed = {

			original_tag = SOV

		}

		available = {

			has_equipment = { small_plane_airframe > 99 }

			has_equipment = { medium_plane_airframe > 49 }

		}

		visible = {

			has_country_flag = CHI_soviet_volunteer_group

		}

		cost = 25

		ai_will_do = {

			factor = 1

		}

		days_remove = -1

		fire_only_once = yes

		complete_effect = {

			random_country = {

				limit = {

					has_completed_focus = CHI_the_soviet_volunteer_group

				}

				custom_effect_tooltip = soviet_volunteers_tt

				hidden_effect = {

					if = {

						limit = {

							NOT = {

								has_dlc = "By Blood Alone"

							}

						}

						add_equipment_to_stockpile = {

							producer = SOV

							type = fighter_equipment_1

							amount = 100

						}

						add_equipment_to_stockpile = {

							producer = SOV

							type = tac_bomber_equipment_1

							amount = 50

						}	

					}

					else = {

						add_equipment_to_stockpile = {

							producer = SOV

							type = small_plane_airframe_1

							amount = 100

							variant_name = "I-16"

						}

						add_equipment_to_stockpile = {

							producer = SOV

							type = medium_plane_airframe_0

							amount = 50

							variant_name = "DB-3"

						}	

					}

				}

			}

			add_war_support = 0.05	

		}

		modifier = {

			air_volunteer_cap = 1

		}

	}



    CHI_rcm_close_burma_road = { #关闭滇缅公路

		icon = GFX_decision_generic_motorized

		allowed = {

			OR = {

				original_tag = ENG

				original_tag = BUR

				original_tag = RAJ

			}

		}

		available = {

			is_subject = no

			OR = {

				992 = {

					controller = {

						is_subject_of = ROOT

					}

				}

				controls_state = 992

			}

			NOT = {

				has_global_flag = burma_road_closed_flag

			}

			if = {

				limit = {

					original_tag = ENG

				}

				NOT = {

					has_global_flag = sino_british_agreement_flag

				}

			}

		}

		visible = {

			has_global_flag = burma_road_done_flag

		}

		

		cost = 10



		ai_will_do = {

			base = 0

			modifier = {

				add = 1000

				any_enemy_country = {

					is_literally_china = yes

					OR = {

						is_ally_with = event_target:WTT_current_china_leader

						tag = event_target:WTT_current_china_leader

					}

				}

			}

		}

		

		fire_only_once = no



		days_re_enable = 1

		

		#days_remove = 365

		

		complete_effect = {

			set_global_flag = burma_road_closed_flag

			random_other_country = {

				limit = {

					has_country_flag = chi_eng_agreed_burma_road_flag

				}

				country_event = { id = wtt_burma_road.2 }

				effect_tooltip = {

					add_offsite_building = {

						type = arms_factory

						level = -4

					}

				}

			}

		}

	} #MODDED



	CHI_rcm_open_burma_road = { #重启滇缅公路

		icon = GFX_decision_generic_motorized

		allowed = {

			OR = {

				original_tag = ENG

				original_tag = BUR

				original_tag = RAJ

			}

		}

		available = {

			has_global_flag = burma_road_closed_flag

		}

		visible = {

			has_global_flag = burma_road_done_flag

			if = {

				limit = {

					is_subject = no

				}

				992 = {

				    controller = {

						OR = {

							is_subject = ROOT

							tag = ROOT

						}

					}

				}

			}

			747 = {

				is_not_controlled_by_jap_or_its_puppet = yes

				controller = {

					NOT = {

						has_war_with = ROOT

					}

				}

				OR = {

					controller = {

						is_legit_china = yes

					}

					controller = {

						is_subject_of = event_target:WTT_current_china_leader

					}

					controller = {

						is_in_faction_with = event_target:WTT_current_china_leader

					}	

				}

			}

			640 = {

				is_not_controlled_by_jap_or_its_puppet = yes

				controller = {

					NOT = {

						has_war_with = ROOT

					}

				}

				OR = {

					controller = {

						is_legit_china = yes

					}

					controller = {

						is_subject_of = event_target:WTT_current_china_leader

					}

					controller = {

						is_in_faction_with = event_target:WTT_current_china_leader

					}	

				}

			}	

		}

		

		cost = 10



		ai_will_do = {

			factor = 100

		}

		

		fire_only_once = no

		

        days_re_enable = 1



		#days_remove = 365

		

		complete_effect = {

			clr_global_flag = burma_road_closed_flag

			random_other_country = {

				limit = {

					has_country_flag = chi_eng_agreed_burma_road_flag

				}

				country_event = { id = wtt_burma_road.4 }

				add_offsite_building = {

					type = arms_factory

					level = 4

				}

			}

		}

	} #MODDED

	#REMOVED BURMA ROAD for better performance

		#REMOVE the hump

}

operations = { #行动

#USA sends hull note

RCM_nuke_japan_decision = { #核爆选择

		icon = eng_trade_unions_support

		fire_only_once = yes

		days_remove = 7 #GUO # Stays for 5 days before being removed #MODDED

		

		cost = 0

		highlight_states = { #These states will be highlighted

			state = 529

			state = 528

		}

		allowed = {

			original_tag = USA

		}

		visible = { #can see

			has_war_with = JAP

			has_tech = nukes

		}

		available = { #can do

			has_tech = nukes

			JAP = { #日本

				surrender_progress < 0.1

			}

			if = {

			limit = {

			has_war_with = GER

			has_idea = lianheguojiaxuanyan

			ENG = { #英国

			SOV = { #俄罗斯

			has_global_flag = kairo_meeting_happen

			has_global_flag = tehran_meeting_happen

			}

			has_global_flag = Potsdam_Declaration_happen

			}

			else = { always = yes }

			

			if = {

			limit = { is_ai = no }

			num_of_nukes > 1

			}

			else = { always = yes }

			

			OR = {

				and = {

					OR = {

						controls_state = 526

						controls_state = 645

						controls_state = 646

						controls_state = 648

						526 = { 

							controller = {

								has_war_with = JAP

							}

						}

						645 = { 

							controller = {

								has_war_with = JAP

							}

						}

						646 = { 

							controller = {

								has_war_with = JAP

							}

						}

						648 = { 

							controller = {

								has_war_with = JAP

							}

						}

					}

				}

				and = {

					is_ai = yes

					JAP = { #日本

						is_ai = yes

					}

					OR = {

						AND = {

                            SOV = { #俄罗斯

								has_war_with = MAN 

								has_war_with = JAP

							}

							MAN = { #中国

								surrender_progress > 0.4

							}

						}	

						event_target:WTT_current_china_leader = {

							NOT = {

								has_war_with = JAP

							}

						}

					}

				}

			}

		}

		cancel_trigger = {

		    if = {

			limit = { is_ai = no }

			num_of_nukes < 2

			}

			else = { always = no }

		}

		cancel_if_not_visible = yes

		complete_effect = { #run effect on selection

			#launch_nuke = { 

			#   provinve = 1092 #will nuke this province if specified

			#   state = 529 #use either province or state. if state is used it will prefer enemies first while picking a province to nuke. otherwise it will pick one of the neutrals

			#   controller = JAP #if state and controller is specified, the effect will pick a province that is controlled by this tag

			#   use_nuke = yes #will consume nuke if specified

			#}

			#launch_nuke = { 

			#   provinve = 9950 #will nuke this province if specified

			#   state = 528 #use either province or state. if state is used it will prefer enemies first while picking a province to nuke. otherwise it will pick one of the neutrals

			#   controller = JAP #if state and controller is specified, the effect will pick a province that is controlled by this tag

			#   use_nuke = yes #will consume nuke if specified

			#}

			add_manpower = -2

			set_global_flag = RCM_nuke_japan_decision_happen_zz

			#add_equipment_to_stockpile = {

			#	type = strat_bomber_equipment_2

			#	amount = -2

			#}

		}

		remove_trigger = { #will run remove effect and abort



		}

		remove_effect = { #run effect when removed/finished

			if = {

				limit = {

					NOT = {

						has_country_flag = nuke_a_flag

					}

					is_ai = no

				}

				set_country_flag = nuke_a_flag

				custom_effect_tooltip = nuke_japan_decision_tt

				launch_nuke = { 

				   province = 1092 #will nuke this province if specified

				   state = 529 #use either province or state. if state is used it will prefer enemies first while picking a province to nuke. otherwise it will pick one of the neutrals

				   controller = JAP #if state and controller is specified, the effect will pick a province that is controlled by this tag

				   use_nuke = yes #will consume nuke if specified

				}

				launch_nuke = { 

				   province = 9950 #will nuke this province if specified

				   state = 528 #use either province or state. if state is used it will prefer enemies first while picking a province to nuke. otherwise it will pick one of the neutrals

				   controller = JAP #if state and controller is specified, the effect will pick a province that is controlled by this tag

				   use_nuke = yes #will consume nuke if specified

				}

				else_if = {

                    limit = {

						NOT = {

							has_country_flag = nuke_a_flag

						}

						is_ai = yes

					}

					set_country_flag = nuke_a_flag

				    custom_effect_tooltip = nuke_japan_decision_tt

					launch_nuke = { 

						province = 1092 #will nuke this province if specified

						state = 529 #use either province or state. if state is used it will prefer enemies first while picking a province to nuke. otherwise it will pick one of the neutrals

						controller = JAP #if state and controller is specified, the effect will pick a province that is controlled by this tag

						use_nuke = no #will consume nuke if specified

					}

					launch_nuke = { 

						province = 9950 #will nuke this province if specified

						state = 528 #use either province or state. if state is used it will prefer enemies first while picking a province to nuke. otherwise it will pick one of the neutrals

						controller = JAP #if state and controller is specified, the effect will pick a province that is controlled by this tag

						use_nuke = no #will consume nuke if specified

					}

					if = {

						limit = {

							NOT = {

                                has_global_flag = JAP_nuke_1

							}

						}

						set_global_flag = JAP_nuke_1

						set_global_flag = JAP_nuke_2

						set_country_flag = I_drop_first_nuke

						set_country_flag = I_drop_second_nuke

					}



				}

				else_if = {

                    limit = {

						has_country_flag = nuke_a_flag

					}

					launch_nuke = { 

						#province = 1092 #will nuke this province if specified

						#state = 529 #use either province or state. if state is used it will prefer enemies first while picking a province to nuke. otherwise it will pick one of the neutrals

						controller = JAP #if state and controller is specified, the effect will pick a province that is controlled by this tag

						use_nuke = yes #will consume nuke if specified

					}

				    launch_nuke = { 

						#province = 9950 #will nuke this province if specified

						#state = 528 #use either province or state. if state is used it will prefer enemies first while picking a province to nuke. otherwise it will pick one of the neutrals

						controller = JAP #if state and controller is specified, the effect will pick a province that is controlled by this tag

						use_nuke = yes #will consume nuke if specified

					}

				}

			}

			

			set_global_flag = first_nuke_dropped

			hidden_effect = { country_event = super.18 }

			

			#add_manpower = 12

			#add_equipment_to_stockpile = {

			#	type = strat_bomber_equipment_2

			#	amount = 12

			#}

			#custom_effect_tooltip = goes_away_when_at_war_with_US_tt #uncomment this to reenable ES#

			#uncomment this to reenable ES#custom_effect_tooltip = this_will_cause_inflation_as_if_we_are_at_war_with_the_USA_TT

		}

		#war_with_target_on_remove = yes

		modifier = { #active during the course of the decision

		}

		ai_will_do = { #guo

			base = 0

			modifier = {

				add = 9999

				date > 1945.8.1

			}

			modifier = {

				add = 9999

				JAP = { #日本

				any_core_state = {

				region = 154

				controller = { has_War_with = JAP }

				}

				}

			}

			modifier = {

				add = 9999

				SOV = { #俄罗斯

			}

			modifier = {

				add = 9999

				or = {

				event_target:WTT_current_china_leader = { surrender_progress < 0.05 }

				has_global_flag = JAP_defeated

				}

			}

		}

	}

}
